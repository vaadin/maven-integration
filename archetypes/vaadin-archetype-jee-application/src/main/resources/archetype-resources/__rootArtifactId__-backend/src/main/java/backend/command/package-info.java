#set( $symbol_pound = '#' )
#set( $symbol_dollar = '$' )
#set( $symbol_escape = '\' )
/**
 * This is an example of a command based backend, where each operation is implemented as a command-command handler pair.
 * The command handlers are managed CDI beans. If you don't want to use commands in your application, delete
 * this package.
 */
package ${package}.backend.command;